{
  "name": "claudio",
  "version": "0.1.0",
  "private": true,
  "description": "Claudio — paródia de assistente de IA. Inteligência Aproximada, resultados surpreendentes.",
  "type": "module",
  "scripts": {
    "dev": "npm --prefix worker run dev",
    "deploy": "npm --prefix worker run deploy",
    "secret": "npm --prefix worker run secret",
    "tail": "npm --prefix worker run tail",
    "test": "node --test \"tests/**/*.test.js\"",
    "bench": "node bench/run.js",
    "postinstall": "npm --prefix worker install --no-audit --no-fund"
  },
  "engines": {
    "node": ">=20"
  }
}
